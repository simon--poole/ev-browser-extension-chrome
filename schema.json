{
    "type": "object",
    // "properties" maps an optional key of this object to its schema. At the
    // top-level object, these keys are the policy names supported.
    "properties": {
        // The policy name "AutoSave" is mapped to its schema, which in this case
        // declares it as a simple boolean value.
        // "title" and "description" are optional and are used to show a
        // user-friendly name and documentation to the administrator.
        "related": {
            "title": "Hide related videos.",
            "description": "If set to true then related videos will be hidden by default.",
            "type": "boolean"
        },
        "comments": {
            "title": "Hide video comments.",
            "description": "If set to true then video comments will be hidden by default.",
            "type": "boolean"
        },
        "length": {
            "title": "Hide video length.",
            "description": "If set to true then video length will be hidden by default.",
            "type": "boolean"
        }
    }
}